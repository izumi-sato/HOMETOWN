<div class="contents row">
  <div class="content_post>
    <% if user_signed_in? %>
      <div class="more">
        <span></span>
        <ul class="more_list">
          <li>
            <%= link_to '編集', "/ivents/#{@ivent.id}/edit", method: :get %>
          </li>
          <li>
            <%= link_to '削除', "/ivents/#{@ivent.id}", method: :delete %>
          </li>
        </ul>
      </div>
    <% end %>
    <%= simple_format(@ivent.text) %>
  </div>
</div>